[project]
name = "ambient-player"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.119.1", "uvicorn[standard]", "python-vlc"]
[project.scripts]
ambient_player = "main:app"
